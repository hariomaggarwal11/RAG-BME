# Web app + real RAG dependencies
streamlit>=1.30
pypdf>=4.0
sentence-transformers>=2.2

# Optional providers (install only what you need):
# openai>=1.0                 # for OpenAI answer generation (set OPENAI_API_KEY)
# psycopg[binary]>=3.1        # for pgvector persistence
# pgvector>=0.2               # for pgvector persistence
